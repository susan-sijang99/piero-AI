\"\"\"Game Manager - 게임 전체 흐름 관리\"\"\"\n\nfrom character.protagonist import Protagonist, Gender, create_protagonist\nfrom character.piero import Piero, create_piero\nfrom character.harlequin import Harlequin, create_harlequin\nfrom dialogue.dialogue_system import DialogueManager, DialogueNode\nfrom dialogue.pierrot_dialogues import load_pierrot_dialogues\nfrom dialogue.harlequin_dialogues import load_harlequin_dialogues\nfrom dataclasses import dataclass\nfrom typing import Dict, Optional, List\nfrom enum import Enum\n\n\nclass GameState(Enum):\n    \"\"\"게임 상태\"\"\"\n    MENU = \"menu\"\n    CHARACTER_CREATION = \"character_creation\"\n    FIRST_MEETING = \"first_meeting\"\n    DIALOGUE = \"dialogue\"\n    CHOICE = \"choice\"\n    ENDING = \"ending\"\n    GAME_OVER = \"game_over\"\n\n\n@dataclass\nclass GameSession:\n    \"\"\"게임 세션\"\"\"\n    protagonist: Protagonist\n    pierrot: Piero\n    harlequin: Harlequin\n    dialogue_manager: DialogueManager\n    current_state: GameState = GameState.CHARACTER_CREATION\n    current_dialogue_id: Optional[str] = None\n    dialogue_history: List[str] = None\n    choices_made: int = 0\n    \n    def __post_init__(self):\n        if self.dialogue_history is None:\n            self.dialogue_history = []\n\n\nclass GameManager:\n    \"\"\"게임 관리자 - 전체 게임 흐름 관리\"\"\"\n    \n    def __init__(self):\n        self.session: Optional[GameSession] = None\n        self.game_over = False\n    \n    def initialize_game(self) -> GameSession:\n        \"\"\"게임 초기화\"\"\"\n        # 캐릭터 생성\n        protagonist = create_protagonist(\"You\", Gender.FEMALE)\n        pierrot = create_piero()\n        harlequin = create_harlequin()\n        \n        # 대화 시스템 생성\n        dialogue_manager = DialogueManager()\n        load_pierrot_dialogues(dialogue_manager)\n        load_harlequin_dialogues(dialogue_manager)\n        \n        # 세션 생성\n        self.session = GameSession(\n            protagonist=protagonist,\n            pierrot=pierrot,\n            harlequin=harlequin,\n            dialogue_manager=dialogue_manager,\n            current_state=GameState.MENU\n        )\n        \n        return self.session\n    \n    def set_protagonist(self, name: str, gender: Gender) -> None:\n        \"\"\"주인공 설정\"\"\"\n        if not self.session:\n            self.initialize_game()\n        \n        self.session.protagonist = create_protagonist(name, gender)\n    \n    def get_protagonist_stats(self) -> Dict:\n        \"\"\"주인공 통계 반환\"\"\"\n        if not self.session:\n            return {}\n        \n        protagonist = self.session.protagonist\n        return {\n            \"affection\": protagonist.relationships.pierrot_affection,\n            \"fear\": protagonist.emotional_state.fear_level,\n            \"confusion\": protagonist.emotional_state.confusion_level,\n            \"fascination\": protagonist.emotional_state.fascination_level,\n            \"trust\": protagonist.emotional_state.trust_level,\n            \"progress\": protagonist.choices.story_progress\n        }\n    \n    def start_dialogue(self, dialogue_id: str) -> Optional[DialogueNode]:\n        \"\"\"대화 시작\"\"\"\n        if not self.session:\n            return None\n        \n        protagonist_stats = self.get_protagonist_stats()\n        dialogue = self.session.dialogue_manager.start_dialogue(\n            dialogue_id,\n            protagonist_stats\n        )\n        \n        if dialogue:\n            self.session.current_dialogue_id = dialogue_id\n            self.session.dialogue_history.append(dialogue_id)\n            self.session.current_state = GameState.DIALOGUE\n            return dialogue\n        \n        return None\n    \n    def get_current_dialogue(self) -> Optional[DialogueNode]:\n        \"\"\"현재 대화 반환\"\"\"\n        if not self.session or not self.session.current_dialogue_id:\n            return None\n        \n        return self.session.dialogue_manager.get_dialogue(\n            self.session.current_dialogue_id\n        )\n    \n    def get_available_choices(self) -> List:\n        \"\"\"사용 가능한 선택지 반환\"\"\"\n        if not self.session:\n            return []\n        \n        dialogue = self.get_current_dialogue()\n        if not dialogue:\n            return []\n        \n        protagonist_stats = self.get_protagonist_stats()\n        return self.session.dialogue_manager.get_available_choices(\n            dialogue,\n            protagonist_stats\n        )\n    \n    def process_choice(self, choice_index: int) -> Dict:\n        \"\"\"선택지 처리\"\"\"\n        if not self.session:\n            return {\"error\": \"No active session\"}\n        \n        dialogue = self.get_current_dialogue()\n        if not dialogue:\n            return {\"error\": \"No current dialogue\"}\n        \n        available_choices = self.get_available_choices()\n        if choice_index < 0 or choice_index >= len(available_choices):\n            return {\"error\": \"Invalid choice index\"}\n        \n        choice = available_choices[choice_index]\n        \n        # 선택 처리\n        protagonist_stats = self.get_protagonist_stats()\n        consequences = self.session.dialogue_manager.process_choice(\n            choice,\n            protagonist_stats\n        )\n        \n        # 주인공 통계 업데이트\n        if consequences[\"affection_change\"] != 0:\n            if dialogue.speaker == \"Pierrot\":\n                self.session.protagonist.relationships.pierrot_affection = max(\n                    0,\n                    min(\n                        100,\n                        self.session.protagonist.relationships.pierrot_affection + \n                        consequences[\"affection_change\"]\n                    )\n                )\n            elif dialogue.speaker == \"Harlequin\":\n                self.session.protagonist.relationships.harlequin_interest_level = max(\n                    0,\n                    min(\n                        100,\n                        self.session.protagonist.relationships.harlequin_interest_level + \n                        consequences[\"affection_change\"]\n                    )\n                )\n        \n        if consequences[\"fear_change\"] != 0:\n            self.session.protagonist.emotional_state.update_fear(\n                consequences[\"fear_change\"]\n            )\n        \n        if consequences[\"fascination_change\"] != 0:\n            self.session.protagonist.emotional_state.update_fascination(\n                consequences[\"fascination_change\"]\n            )\n        \n        # 선택 기록\n        self.session.protagonist.make_choice(\n            choice.text,\n            self.session.choices_made,\n            consequences\n        )\n        self.session.choices_made += 1\n        \n        # 다음 대화로 이동\n        next_dialogue_id = choice.next_dialogue_id\n        \n        return {\n            \"choice_text\": choice.text,\n            \"consequences\": consequences,\n            \"next_dialogue_id\": next_dialogue_id,\n            \"next_dialogue\": self.start_dialogue(next_dialogue_id)\n        }\n    \n    def check_ending_condition(self) -> Optional[str]:\n        \"\"\"엔딩 조건 확인\"\"\"\n        if not self.session:\n            return None\n        \n        ending = self.session.protagonist.check_ending_condition()\n        \n        if ending:\n            self.session.current_state = GameState.ENDING\n        \n        return ending\n    \n    def get_game_status(self) -> Dict:\n        \"\"\"현재 게임 상태 반환\"\"\"\n        if not self.session:\n            return {\"status\": \"No active session\"}\n        \n        protagonist = self.session.protagonist\n        \n        return {\n            \"protagonist_name\": protagonist.name,\n            \"protagonist_gender\": protagonist.gender.value,\n            \"current_state\": self.session.current_state.value,\n            \"current_dialogue_id\": self.session.current_dialogue_id,\n            \"stats\": self.get_protagonist_stats(),\n            \"pierrot_affection\": protagonist.relationships.pierrot_affection,\n            \"harlequin_interest\": protagonist.relationships.harlequin_interest_level,\n            \"choices_made\": self.session.choices_made,\n            \"story_progress\": protagonist.choices.story_progress,\n            \"potential_ending\": self.check_ending_condition()\n        }\n    \n    def trigger_first_meeting(self) -> Optional[DialogueNode]:\n        \"\"\"첫 만남 트리거\"\"\"\n        if not self.session:\n            return None\n        \n        # 피에로와의 첫 만남\n        encounter = self.session.protagonist.encounter_pierrot()\n        \n        self.session.current_state = GameState.FIRST_MEETING\n        \n        # 첫 대화 시작\n        return self.start_dialogue(\"pierrot_first_meeting_1\")\n    \n    def get_full_game_info(self) -> Dict:\n        \"\"\"전체 게임 정보 반환\"\"\"\n        if not self.session:\n            return {}\n        \n        return {\n            \"protagonist\": self.session.protagonist.get_full_profile(),\n            \"pierrot\": self.session.pierrot.get_full_profile(),\n            \"harlequin\": self.session.harlequin.get_full_profile(),\n            \"game_status\": self.get_game_status(),\n            \"dialogue_count\": len(self.session.dialogue_history)\n        }\n    \n    def end_game(self, reason: str = \"normal\") -> Dict:\n        \"\"\"게임 종료\"\"\"\n        if not self.session:\n            return {\"error\": \"No active session\"}\n        \n        ending_type = self.check_ending_condition()\n        \n        return {\n            \"ending_type\": ending_type,\n            \"reason\": reason,\n            \"protagonist_final_stats\": self.session.protagonist.get_full_profile(),\n            \"choices_made\": self.session.choices_made,\n            \"story_progress\": self.session.protagonist.choices.story_progress\n        }\n\n\n# 게임 매니저 싱글톤\n_game_manager_instance: Optional[GameManager] = None\n\ndef get_game_manager() -> GameManager:\n    \"\"\"게임 매니저 인스턴스 반환\"\"\"\n    global _game_manager_instance\n    if _game_manager_instance is None:\n        _game_manager_instance = GameManager()\n    return _game_manager_instance\n\ndef reset_game_manager():\n    \"\"\"게임 매니저 리셋\"\"\"\n    global _game_manager_instance\n    _game_manager_instance = None\n"