\"\"\"Protagonist Character - 주인공 캐릭터 정보\"\"\"\n\nfrom enum import Enum\nfrom dataclasses import dataclass, field\nfrom typing import Dict, List, Optional\nfrom datetime import datetime\n\n\nclass Gender(Enum):\n    \"\"\"성별 선택\"\"\"\n    MALE = \"male\"              # 남성 (He/Him)\n    FEMALE = \"female\"          # 여성 (She/Her)\n    NON_BINARY = \"non_binary\"  # 논바이너리 (They/Them)\n\n\nclass PronounSet:\n    \"\"\"성별에 따른 대명사 세트\"\"\"\n    MALE = {\n        \"subject\": \"He\",\n        \"object\": \"him\",\n        \"possessive\": \"his\",\n        \"reflexive\": \"himself\",\n        \"verb_form\": \"is\"  # he is\n    }\n    \n    FEMALE = {\n        \"subject\": \"She\",\n        \"object\": \"her\",\n        \"possessive\": \"her\",\n        \"reflexive\": \"herself\",\n        \"verb_form\": \"is\"  # she is\n    }\n    \n    NON_BINARY = {\n        \"subject\": \"They\",\n        \"object\": \"them\",\n        \"possessive\": \"their\",\n        \"reflexive\": \"themselves\",\n        \"verb_form\": \"are\"  # they are\n    }\n    \n    @staticmethod\n    def get_pronouns(gender: Gender) -> Dict[str, str]:\n        \"\"\"성별에 따른 대명사 반환\"\"\"\n        if gender == Gender.MALE:\n            return PronounSet.MALE\n        elif gender == Gender.FEMALE:\n            return PronounSet.FEMALE\n        else:\n            return PronounSet.NON_BINARY\n\n\n@dataclass\nclass ProtagonistAppearance:\n    \"\"\"주인공의 외모 정보\"\"\"\n    gender: Gender = Gender.MALE\n    \n    # 기본 외모\n    skin_tone: str = \"grayish\"                      # 회색빛 피부\n    height: int = 170                              # 평균 키\n    \n    # 캐주얼 의상\n    casual_top: str = \"black top\"                  # 검은색 상의\n    casual_bottom: str = \"blue pants\"              # 파란색 바지\n    casual_style: str = \"neat and tidy\"            # 단정한 차림\n    \n    # 카페 유니폼\n    work_uniform: str = \"cafe employee uniform\"    # 카페 직원 유니폼\n    work_appearance: str = \"professional\"          # 전문적인 모습\n    \n    # 특징\n    distinguishing_features: List[str] = field(default_factory=lambda: [\n        \"ordinary appearance\",\n        \"unremarkable features\",\n        \"human\"\n    ])\n    \n    # 상징색\n    symbol_color: str = \"red\"                      # 붉은색\n    \n    def get_pronouns(self) -> Dict[str, str]:\n        \"\"\"성별에 따른 대명사 반환\"\"\"\n        return PronounSet.get_pronouns(self.gender)\n    \n    def to_dict(self) -> Dict:\n        return {\n            \"gender\": self.gender.value,\n            \"pronouns\": self.get_pronouns(),\n            \"skin_tone\": self.skin_tone,\n            \"height_cm\": self.height,\n            \"casual_appearance\": {\n                \"top\": self.casual_top,\n                \"bottom\": self.casual_bottom,\n                \"style\": self.casual_style\n            },\n            \"work_appearance\": {\n                \"uniform\": self.work_uniform,\n                \"style\": self.work_appearance\n            },\n            \"symbol_color\": self.symbol_color\n        }\n\n\n@dataclass\nclass ProtagonistBackground:\n    \"\"\"주인공의 배경 정보\"\"\"\n    # 기본 정보\n    name: str = \"Protagonist\"           # 플레이어가 지정 가능\n    species: str = \"human\"\n    \n    # 나이\n    estimated_age: str = \"20s\"          # 정확한 나이는 미정\n    minimum_age: int = 20               # 최소 성인\n    \n    # 직업\n    occupation: str = \"Cafe employee\"   # 카페 직원\n    employment_status: str = \"part-time\"\n    \n    # 거주 지역\n    residence: str = \"Near the circus town\"  # 서커스 근처 마을\n    commute_location: str = \"Cafe\"          # 카페로 출근\n    \n    # 성격\n    personality_type: str = \"ordinary\"      # 평범한 성격\n    description: str = \"normal human\"\n    \n    # 특수성\n    special_abilities: List[str] = field(default_factory=list)  # 특별한 능력 없음\n    \n    # 이야기 역할\n    role_in_story: str = \"Center of the plot\"\n    story_description: str = \"\"\"Unwillingly drawn into The Freak Circus's mysteries,\n                                  becomes the object of affection and obsession for two monsters,\n                                  and must navigate dangerous relationships while uncovering dark secrets\"\"\"\n    \n    def to_dict(self) -> Dict:\n        return {\n            \"name\": self.name,\n            \"species\": self.species,\n            \"estimated_age\": self.estimated_age,\n            \"minimum_age\": self.minimum_age,\n            \"occupation\": self.occupation,\n            \"employment_status\": self.employment_status,\n            \"residence\": self.residence,\n            \"personality\": self.personality_type,\n            \"abilities\": self.special_abilities if self.special_abilities else \"None - ordinary human\",\n            \"role_in_story\": self.role_in_story\n        }\n\n\n@dataclass\nclass ProtagonistEmotionalState:\n    \"\"\"주인공의 감정 상태\"\"\"\n    # 기본 감정\n    fear_level: int = 50                           # 공포 (0~100)\n    confusion_level: int = 60                      # 혼란 (0~100)\n    fascination_level: int = 40                    # 매력 (0~100)\n    trust_level: int = 20                          # 신뢰 (0~100)\n    \n    # 각 캐릭터에 대한 감정\n    feeling_about_pierrot: str = \"terrified and fascinated\"     # 피에로에 대한 감정\n    feeling_about_harlequin: str = \"cautious and intrigued\"     # 할리퀸에 대한 감정\n    \n    # 심리 상태\n    is_traumatized: bool = False                   # 트라우마\n    psychological_state: str = \"normal but confused\"\n    \n    def update_fear(self, amount: int):\n        \"\"\"공포 수준 업데이트\"\"\"\n        self.fear_level = max(0, min(100, self.fear_level + amount))\n    \n    def update_fascination(self, amount: int):\n        \"\"\"매력 수준 업데이트\"\"\"\n        self.fascination_level = max(0, min(100, self.fascination_level + amount))\n    \n    def to_dict(self) -> Dict:\n        return {\n            \"fear_level\": self.fear_level,\n            \"confusion_level\": self.confusion_level,\n            \"fascination_level\": self.fascination_level,\n            \"trust_level\": self.trust_level,\n            \"feelings\": {\n                \"about_pierrot\": self.feeling_about_pierrot,\n                \"about_harlequin\": self.feeling_about_harlequin\n            },\n            \"psychological_state\": self.psychological_state,\n            \"is_traumatized\": self.is_traumatized\n        }\n\n\n@dataclass\nclass ProtagonistRelationships:\n    \"\"\"주인공의 관계 정보\"\"\"\n    # 피에로와의 관계\n    pierrot_affection: int = 0                     # 피에로의 호감도 (영향을 받는 것)\n    pierrot_obsession_level: int = 70              # 피에로의 집착 수준\n    pierrot_relationship_type: str = \"forced proximity\"  # 관계 유형\n    \n    # 할리퀸과의 관계\n    harlequin_interest_level: int = 50             # 할리퀸의 관심 수준\n    harlequin_mischief_level: int = 70             # 할리퀸의 장난기\n    harlequin_relationship_type: str = \"temptation\"     # 관계 유형\n    \n    # 서커스와의 관계\n    circus_involvement: int = 0                    # 서커스 관여도 (0~100)\n    circus_knowledge: int = 0                      # 서커스에 대한 지식\n    circus_danger_awareness: int = 50              # 서커스 위험 인식\n    \n    # 감정 기록\n    interaction_history: List[Dict] = field(default_factory=list)\n    \n    def record_interaction(self, character: str, event: str, affection_change: int = 0):\n        \"\"\"상호작용 기록\"\"\"\n        self.interaction_history.append({\n            \"character\": character,\n            \"event\": event,\n            \"timestamp\": datetime.now().isoformat(),\n            \"affection_change\": affection_change\n        })\n    \n    def to_dict(self) -> Dict:\n        return {\n            \"pierrot\": {\n                \"affection\": self.pierrot_affection,\n                \"obsession_level\": self.pierrot_obsession_level,\n                \"relationship_type\": self.pierrot_relationship_type\n            },\n            \"harlequin\": {\n                \"interest_level\": self.harlequin_interest_level,\n                \"mischief_level\": self.harlequin_mischief_level,\n                \"relationship_type\": self.harlequin_relationship_type\n            },\n            \"circus\": {\n                \"involvement\": self.circus_involvement,\n                \"knowledge\": self.circus_knowledge,\n                \"danger_awareness\": self.circus_danger_awareness\n            },\n            \"interaction_count\": len(self.interaction_history)\n        }\n\n\n@dataclass\nclass ProtagonistChoices:\n    \"\"\"주인공의 선택 기록\"\"\"\n    # 중요한 선택들\n    choices_made: List[Dict] = field(default_factory=list)\n    \n    # 경로 선택\n    current_path: str = \"neutral\"                  # \"pierrot_route\", \"harlequin_route\", \"both_route\", \"escape_route\"\n    \n    # 스토리 진행\n    story_progress: int = 0                        # 0~100%\n    \n    def record_choice(self, choice_description: str, consequences: Dict):\n        \"\"\"선택 기록\"\"\"\n        self.choices_made.append({\n            \"choice\": choice_description,\n            \"consequences\": consequences,\n            \"timestamp\": datetime.now().isoformat()\n        })\n    \n    def to_dict(self) -> Dict:\n        return {\n            \"total_choices_made\": len(self.choices_made),\n            \"current_path\": self.current_path,\n            \"story_progress\": self.story_progress,\n            \"recent_choices\": self.choices_made[-5:] if self.choices_made else []\n        }\n\n\nclass Protagonist:\n    \"\"\"주인공 - 완전한 플레이어 캐릭터 클래스\"\"\"\n    \n    def __init__(self, name: str = \"You\", gender: Gender = Gender.MALE):\n        # 기본 정보\n        self.name = name\n        self.gender = gender\n        \n        # 외모\n        self.appearance = ProtagonistAppearance(gender=gender)\n        \n        # 배경\n        self.background = ProtagonistBackground(name=name)\n        \n        # 감정 상태\n        self.emotional_state = ProtagonistEmotionalState()\n        \n        # 관계\n        self.relationships = ProtagonistRelationships()\n        \n        # 선택\n        self.choices = ProtagonistChoices()\n        \n        # 게임 상태\n        self.is_alive = True\n        self.is_escaped = False\n        self.ending_type: Optional[str] = None  # \"pierrot_ending\", \"harlequin_ending\", \"both_ending\", \"escape_ending\", \"bad_ending\"\n    \n    def get_pronouns(self) -> Dict[str, str]:\n        \"\"\"성별에 따른 대명사 반환\"\"\"\n        return self.appearance.get_pronouns()\n    \n    def encounter_pierrot(self) -> Dict:\n        \"\"\"피에로와의 첫 만남\"\"\"\n        self.relationships.pierrot_affection = 50  # 첫 만남 호감도\n        self.relationships.record_interaction(\n            \"Pierrot\",\n            \"First encounter at cafe\",\n            affection_change=50\n        )\n        self.emotional_state.update_fascination(20)\n        self.emotional_state.update_fear(30)\n        \n        return {\n            \"event\": \"First encounter with Pierrot\",\n            \"location\": \"On the way to work (cafe)\",\n            \"consequence\": \"Drawn into The Freak Circus\",\n            \"pierrot_obsession_triggered\": True\n        }\n    \n    def encounter_harlequin(self) -> Dict:\n        \"\"\"할리퀸과의 만남\"\"\"\n        self.relationships.harlequin_interest_level = 60\n        self.relationships.record_interaction(\n            \"Harlequin\",\n            \"Encounter with Harlequin\",\n            affection_change=0  # 할리퀸은 호감도가 아닌 관심도\n        )\n        self.emotional_state.update_confusion(20)\n        self.emotional_state.update_fascination(25)\n        \n        return {\n            \"event\": \"Encounter with Harlequin\",\n            \"consequence\": \"Rivalry between Pierrot and Harlequin begins\",\n            \"love_triangle_formed\": True\n        }\n    \n    def make_choice(self, choice_text: str, choice_index: int, consequences: Dict):\n        \"\"\"플레이어의 선택\"\"\"\n        self.choices.record_choice(f\"Choice #{choice_index}: {choice_text}\", consequences)\n        \n        # 스토리 진행도 업데이트\n        self.choices.story_progress = min(100, self.choices.story_progress + 10)\n    \n    def get_relationship_status(self) -> Dict:\n        \"\"\"현재 관계 상태 반환\"\"\"\n        return {\n            \"with_pierrot\": {\n                \"affection\": self.relationships.pierrot_affection,\n                \"obsession_level\": self.relationships.pierrot_obsession_level,\n                \"status\": self.relationships.pierrot_relationship_type\n            },\n            \"with_harlequin\": {\n                \"interest_level\": self.relationships.harlequin_interest_level,\n                \"mischief_level\": self.relationships.harlequin_mischief_level,\n                \"status\": self.relationships.harlequin_relationship_type\n            }\n        }\n    \n    def get_emotional_state(self) -> Dict:\n        \"\"\"현재 감정 상태 반환\"\"\"\n        return self.emotional_state.to_dict()\n    \n    def get_full_profile(self) -> Dict:\n        \"\"\"주인공의 완전한 프로필 반환\"\"\"\n        pronouns = self.get_pronouns()\n        return {\n            \"name\": self.name,\n            \"pronouns\": pronouns,\n            \"gender\": self.gender.value,\n            \"appearance\": self.appearance.to_dict(),\n            \"background\": self.background.to_dict(),\n            \"emotional_state\": self.emotional_state.to_dict(),\n            \"relationships\": self.relationships.to_dict(),\n            \"choices\": self.choices.to_dict(),\n            \"game_status\": {\n                \"is_alive\": self.is_alive,\n                \"is_escaped\": self.is_escaped,\n                \"ending_type\": self.ending_type\n            }\n        }\n    \n    def check_ending_condition(self) -> Optional[str]:\n        \"\"\"엔딩 조건 확인\"\"\"\n        # 죽음\n        if not self.is_alive:\n            return \"bad_ending\"\n        \n        # 탈출\n        if self.is_escaped:\n            return \"escape_ending\"\n        \n        # 피에로 루트\n        if self.relationships.pierrot_affection >= 80 and self.relationships.harlequin_interest_level < 40:\n            return \"pierrot_ending\"\n        \n        # 할리퀸 루트\n        if self.relationships.harlequin_interest_level >= 80 and self.relationships.pierrot_affection < 40:\n            return \"harlequin_ending\"\n        \n        # 둘 다 엔딩\n        if self.relationships.pierrot_affection >= 60 and self.relationships.harlequin_interest_level >= 60:\n            return \"both_ending\"\n        \n        return None\n    \n    def __repr__(self) -> str:\n        pronouns = self.get_pronouns()\n        return (\n            f\"Protagonist(\"\n            f\"Name={self.name}, \"\n            f\"Gender={self.gender.value}, \"\n            f\"Pronouns={pronouns['subject']}, \"\n            f\"Age=20s, \"\n            f\"Path={self.choices.current_path})\"\n        )\n\n\n# 기본 주인공 인스턴스 생성 함수\ndef create_protagonist(name: str = \"You\", gender: Gender = Gender.MALE) -> Protagonist:\n    \"\"\"새로운 주인공 캐릭터 인스턴스 생성\"\"\"\n    return Protagonist(name=name, gender=gender)\n"